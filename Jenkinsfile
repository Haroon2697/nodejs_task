// This is a reference Jenkinsfile that can be used to create a Freestyle project manually
// For the Jenkins configuration, use these commands in the Execute Shell step

/*
# Install Node.js if not available (only needed if Node.js is not installed on the Jenkins agent)
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# nvm install 18
# nvm use 18

# Install dependencies
npm install

# Run the application
node app.js
*/ 